# 系统运行说明书（郑航体育微信 H5 自动预约）

本项目包含两部分：

- `server/`、`client/`：Node.js + Redis 的“本地抢号系统”（用于压测/库存占位/定时触发等）。
- `python/reserve_hy_wechat_h5.py`：直接对接 `hy.weiyouyuan.com.cn` 的**微信 H5 真实预约脚本**（本说明书重点）。

> 说明：`http://hy.weiyouyuan.com.cn/mobile/index.aspx` 只能在微信内打开。本脚本通过复刻微信请求头与 Cookie，实现 PC 端自动提交预约。

---

## 1. 环境要求

- Windows 10/11
- Python 3.10+（你当前是 3.10.x 即可）

Python 依赖：

```bash
python -m pip install -r python/requirements.txt
```

---

## 2. 配置说明（必须）

编辑文件：`python/reserve_hy_wechat_h5.py`，在 `WeChatSessionConfig` 中填写/更新：

- **会话信息（最重要）**
  - `cookie`：必须包含
    - `hy_openid=...`
    - `ASP.NET_SessionId=...`
  - `user_agent`：需包含 `MicroMessenger/...`（微信内置浏览器特征）
  - `referer`：通常是 `second.aspx?...`（从抓包复制）

- **预约人信息**
  - `tel`、`realname`、`sex`、`userno`

- **预约目标**
  - `cgid`：场馆（如东体育馆）
  - `ydxmid`：项目（羽毛球/乒乓球等，对应系统里的 ID）
  - `days`：日期（页面格式，如 `03/16`）
  - `cdid`：格子（如 `20_1`，含时段+场地）

### 新用户如何获取配置（抓包指引）

你需要在**微信里**打开“郑航体育 → 场馆预约”，用抓包工具拿到请求头与 Cookie。

推荐抓包方式（任选其一）：

- **Windows 微信（PC 端）**：Fiddler Classic / Charles 抓 `hy.weiyouyuan.com.cn`
- **手机微信**：HttpCanary（安卓）/ Charles（iOS+电脑代理）

抓包时请按这个顺序操作一次：

1. 进入预约表格页（有绿色/橙色/灰色格子的那一页，通常是 `index_1.aspx`）
2. 点击任意一个格子进入填写信息页（`second.aspx?...select_id=xx_x...`）
3. 记录以下三项并填入 `WeChatSessionConfig`：
   - `cookie`：复制请求头中的 `Cookie`，至少要有 `hy_openid` 与 `ASP.NET_SessionId`
   - `user_agent`：复制请求头中的 `User-Agent`（必须包含 `MicroMessenger`）
   - `referer`：复制请求头中的 `Referer`（通常就是 `second.aspx?...`）

然后再填写个人信息与目标参数（`tel/realname/userno/cgid/ydxmid/days`）。`cdid` 可先随便填一个，脚本会在执行预约时自动优先选择 20:00-21:00，其次 18:00-19:00。

### Cookie 过期怎么办

一旦 `state` 长期异常、接口返回跳转/空数据，通常是 Cookie 过期或被替换：

- 重新在微信里打开预约页面
- 用抓包工具抓到新的 `hy_openid` 与 `ASP.NET_SessionId`
- 更新到脚本的 `cookie`

> 建议不要把包含真实 Cookie 的脚本提交到公开仓库。

### 配置有效期是多久（重点）

本脚本的“配置有效期”主要取决于 **Cookie 中的 `ASP.NET_SessionId`（会话）**：

- **通常有效期**：从几十分钟到数小时不等（由服务器配置决定，且可能随时变化）
- **常见失效触发**：
  - 关闭/重登微信、网络切换、长时间不访问导致会话过期
  - 服务器端主动清理 Session
- **建议的使用方式**：
  - 如果你是“每天 6 点准点抢”，建议 **每天抢之前重新抓一次 Cookie**，或至少在开抢前跑一次 `list` 验证 Cookie 是否仍有效

判断 Cookie 是否失效的“现象”：

- `list` 模式里 `get_kyy_list.ashx` 返回空、或一直非预期 JSON
- 访问 `index_1.aspx/second.aspx` 出现跳转到首页/登录页/空白
- 预约提交接口返回异常固定值，且与页面行为不一致

---

## 3. 查询“当前可预约/已预约/不可约”格子（三色）

命令：

```bash
python python/reserve_hy_wechat_h5.py list
```

脚本会：

1. 抓取 `index_1.aspx` 解析出**全部格子**（灰/橙/绿全集）
2. 调用 `get_kyy_list.ashx` 得到 **GREEN（绿色可预约）**
3. 调用 `get_yyy_list.ashx` 得到 **ORANGE（橙色已预约）**
4. 用 **橙色优先覆盖**（若绿橙重叠，最终按橙色）
5. 计算剩余 **GREY（灰色不可约/可能包含已过期）**

当前版本 `list` 只输出：**绿色可预约数量**（用于快速判断是否放号/是否有余量）。

---

## 4. 执行预约（自动解决验证码）

命令：

```bash
python python/reserve_hy_wechat_h5.py
```

验证码说明：

- 站点验证码是“简单算式”，答案范围为 **0–20**
- 脚本采用 **0–20 穷举**绕过识别误差：
  - 先 `GET /yzm/ValidCode.aspx` 生成验证码（写入 Session）
  - 再对 `yzm=0..20` 逐一提交，直到不再返回“验证码错误”

---

## 5. 返回码（state）含义（来自页面 JS）

`save_yy_data.ashx` 的回包通常为 JSON，例如：`{"state":"1","id":"45436"}`

根据 `second.aspx` 页面内 JS 逻辑：

- `state == "1"`：预约成功（会跳转 `result.aspx?id=...`）
- `state == "2"`：该时段已被他人预约
- `state == "3"`：工(学)号不是本校师生，不能预约
- `state == "40"`：验证码错误
- 其他：预约失败（需结合实际提示/抓包进一步确认原因）

---

## 6. 终端日志说明

脚本会对每一步输出日志：

- 请求开始/结束：URL、HTTP 状态码、耗时
- POST 回包预览：`body_preview=...`
- 异常会输出 `[ERROR]` + 完整堆栈，便于定位

### 终端中文乱码（可选处理）

如果 PowerShell 输出中文乱码，可尝试：

```powershell
chcp 65001
$env:PYTHONUTF8=1
python python/reserve_hy_wechat_h5.py list
```

---

## 7. 常见问题排查

- **一直 `state=40`**：验证码未通过（可能 Session 不一致、验证码被刷新、或接口有额外校验）
- **一直 `state=4/其它` 且 `id` 为空**：通常是业务拒绝（参数不匹配、场次不可约、已存在预约、放号时间未到等）
  - 先用 `list` 确认 `cdid` 真的在 GREEN 列表里
  - 再确认 `days/ydxmid/cgid` 与页面一致
- **`get_kyy_list.ashx` 返回空**：多为 Cookie 失效或 Referer/UA 不匹配

